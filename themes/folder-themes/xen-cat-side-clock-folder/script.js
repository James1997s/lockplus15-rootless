(function(){'use strict';var clock=document.getElementById('clock'),date=document.getElementById('date');function pad(n){return n<10?'0'+n:String(n)}function update(){var d=new Date(),h=d.getHours()%12||12;clock.textContent=h+':'+pad(d.getMinutes());date.textContent=d.toLocaleDateString('en-US',{weekday:'short',month:'short',day:'numeric'}).toUpperCase()}function schedule(){update();var d=new Date(),delay=((60-d.getSeconds())*1000)-d.getMilliseconds();setTimeout(schedule,Math.max(250,delay))}schedule()}());
